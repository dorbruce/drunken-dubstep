# drunken-dubstep

no one cares, whatever i write

shift + Z Z -> :wq
o -> leere Zeile + "insert" (i)
a -> rechts neben dem aktuellen buchstaben + "insert" (i)
esc v -> visual mode (markieren)
	- dann x -> ausschneiden
	- dann y -> kopieren
	- dann p -> einfügen
esc shift + V -> analog nur mit ganzen Zeilen
